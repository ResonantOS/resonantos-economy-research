# Upstream Patch Requests

These changes are required before the earlier word-based define package can be treated
as current. They are recorded here instead of silently rewriting approved artifacts.

1. Replace “word” with “short message”: one brief phrase or sentence, candidate maximum
   140 characters.
2. Preserve the limit as at most two messages for each of the 2, 5, and 10-year openings.
3. Describe the outcome as the starting token number—plainly, how many tokens ResonantDAO
   starts with—without presenting the candidate ritual as a production supply protocol.
4. Replace a generic later public value with the candidate Bitcoin closing block hash.
5. Define saved-for-opening messages as per-message commitments included in one master
   manifest fingerprint; reveal the message and its secret proof at the chosen opening.
6. State that a Bitcoin transaction anchors the closing fingerprint, not message content.
7. Keep real saved-message custody and recovery unavailable until separately approved.
8. Stop framing every message as addressed to the community; allow messages to the future
   self, another person, or the community while keeping the capsule itself shared.
9. Keep the message-to-number connection visible in the opening, not only after the form.
