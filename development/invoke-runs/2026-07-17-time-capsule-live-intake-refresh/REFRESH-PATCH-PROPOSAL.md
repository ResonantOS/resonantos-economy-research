# Refresh Patch Proposal — Time Capsule Live Intake

Mutation mode: proposal-only

No patch below has been applied.

## Safe Status Deltas

### Two-hour `INVOKE-RESULT.md`

- Change the lifecycle description from next-route authoring to completed L0
  execution.
- Replace `Task Session SWU-FORM-001` with
  `invoke plan using ../2026-07-17-time-capsule-live-intake-refresh/`.
- Preserve the explicit backend/live-intake and long-term proof gaps.

Signal mapping: RS-01, RS-02, RS-03, RS-10.

### Two-hour `WORK-PACK.md`

- Change top status to `completed within the non-collecting L0 boundary`.
- Change active layer to `L0 closed`.
- Keep every SWU receipt and blocker; do not append backend tasks to this pack.

Signal mapping: RS-01, RS-02.

### Two-hour `IMPLEMENTATION-LAYERING.md`

- Record the L0 machine evidence as passed.
- State that L1 is eligible for revised planning, not execution.
- Link this refresh report as the planning input.

Signal mapping: RS-01, RS-04, RS-10.

## Changes Requiring Plan Authoring

### Live-intake work-pack, execution pack and layering

Do not rewrite these historical high-complexity artifacts with a broad status
patch. Use them as source design refs for `Live Intake Plan V2`.

The revised plan must:

1. map L0 to the completed V2 form and current machine receipts;
2. preserve the human evidence track as a public-live blocker;
3. incorporate D-02–D-06 without calling decisions implementation evidence;
4. replace the initial private-return/sealed branch with a deferred feature;
5. add the selected public-at-opening closing and recovery architecture;
6. keep ritual-number work separate from token policy;
7. define L0–L3 waves, detailed task specs, atomic SWUs, exact validation,
   owner gates, dispatch evidence and Distill validation.

Signal mapping: RS-04, RS-06, RS-07, RS-08, RS-09, RS-10.

### Human access matrix

Regenerate the protocol rather than mechanically replacing labels:

- fixture identity `resonant-time-capsule-intake@2.0.0`;
- short phrases, maximum two per horizon;
- fixed public-at-opening consequence;
- preview receipt is volatile and no server is connected;
- ritual-number explanation uses one shared fingerprint plus the later Bitcoin
  signal;
- quiet path creates no receipt or record;
- five current critical prompts from Fixture V2;
- same all-participant/all-critical pass rule.

Signal mapping: RS-04, RS-05.

### Inventory entry

After the revised plan validates, update the non-authority summary to record:

- L0 V2 machine evidence complete, human evidence still flagged;
- initial release is public at opening only;
- private return is deferred;
- current next route and first selected SWU from `Live Intake Plan V2`.

Signal mapping: RS-01, RS-04, RS-07, RS-10.

## Approval Boundary

Applying the three safe L0 status deltas may be one bounded Task Session. The
high-complexity plan repair belongs to Invoke Plan. The access-matrix update and
Inventory synchronization occur only after the revised contract is available.
