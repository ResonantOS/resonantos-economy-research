# Distill Validation — Live Intake Refresh

## Intent And Budget

- Design intent: reconcile the completed public-at-opening form and current
  decisions with the existing live-intake plan.
- Target context: a safe next route toward live intake without accepting real
  messages prematurely.
- Expected artifact: proposal-only refresh report and patch proposal.
- Optimization goal: identify the smallest coherent next planning unit.
- Mode: Standard, assumed because the user did not select a budget.
- Proposal tracks: one local Proposer/Balancer track.
- Recursive rounds: 2/2.

## Role Conversation Trace

### Round 1

- Proposer: patch the completed L0 pack, append backend tasks and route directly
  to a Node/SQLite Task Session.
- Evidence: D-03 selects the stack and the V2 form passes 32/32 machine checks.
- Balancer objection — authority and closure: the L0 pack is historical and
  complete; the high-complexity plan predates D-15/D-16; human evidence and
  live owner proofs remain open.
- Reconciliation: preserve L0, refresh status only, and route the changed graph
  through Invoke Plan before mutation-capable work.

### Round 2

- Proposer: treat the entire revised live-intake and decade-recovery system as
  the next implementation unit.
- Balancer objection — atomicity and requisite variety: schema/gates,
  synthetic persistence, public operations, close/recovery and Bitcoin ceremony
  have independent acceptance surfaces and owners.
- Reconciliation: the next unit is a revised governed plan; its first candidate
  executable unit is a public-at-opening logical schema/gate/negative-fixture
  freeze using invented data only.

## Smallest Coherent Unit

`Live Intake Plan V2`: one revised Invoke plan that merges:

1. completed L0 V2 evidence;
2. current admission, stack, edit, moderation and retention decisions;
3. public-at-opening-only semantics;
4. operatorless-primary close/recovery and Bitcoin commitment decisions;
5. preserved manual, owner and proof gates.

This unit is planning, not backend implementation. Its first executable SWU
must freeze the public-at-opening schemas, field lifecycle, capability gates and
negative fixtures before writing a live API.

## Closure And Recomposition

- Inputs: paths and signal IDs in the refresh report.
- Output: revised layering, execution pack, detailed task contracts and atomic
  SWUs.
- Closure: the plan can be validated without real content or credentials.
- Recomposition: schema/gates → synthetic Node/SQLite kernel → fenced
  close/export/restore → public-live owner rehearsal → public-at-opening
  bundles/recovery → Bitcoin close and number ceremony.
- No hidden glue: manual comprehension and owner gates remain explicit
  dependencies rather than being inferred from machine evidence.

## Deferred Complexity

- Real participant data and credentials.
- Production deployment and live promotion.
- Private-return encryption or participant recovery keys.
- Mainnet Bitcoin broadcast, real Arweave publication and real 3-of-5 shares.

## Evolution Profile

The initial product evolves across two independent axes: intake reliability and
future-opening durability. The smallest extension boundary keeps their evidence
separate until the close/recovery wave consumes a frozen public-at-opening
manifest.

## Frame Expiry

Repeat Distill if the selected visibility, Node/SQLite stack, closing instant,
operatorless-primary architecture or Bitcoin commitment carrier changes.

## Verdict

Pass for this proposal-only refresh. Mutation-capable execution remains blocked
until Invoke Plan authors and validates `Live Intake Plan V2`.

Next route: `invoke plan`.
