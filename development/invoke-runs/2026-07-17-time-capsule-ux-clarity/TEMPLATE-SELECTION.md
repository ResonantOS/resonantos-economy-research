# Template And Transport Selection

Status: `pass`

## Inventory Lookup

The repository-local machine index was queried first. Two relevant entries were
selected:

- `number-lottery-refinement` — concept, public ritual, and token boundary;
- `time-capsule-live-intake-refine` — form, implementation boundary, and live
  safety blockers.

No inventory entry proves that people understand the current page.

## Invoke Profile

Selected:

- DomainSpec six-view architecture bundle;
- `ux-plan` companion template.

Why: this request needs both a stable interaction architecture and a concrete
screen/state/copy plan. A marketing-page-only template would hide state,
accessibility, and safety behavior. An implementation plan would exceed the
requested lifecycle stage.

## Whisper Transport

Selected: `interactive-web-copy-extension` with `candidate` transport status.

The copy must cover a linked landing page and a progressive interactive form,
including helper text, errors, review, close state, and receipt—not only a
presentation script or campaign headline. This extension is not an
experiment-validated preset, so transport proof remains unproven.

## Distill Budget

Selected: Standard Distill.

The user did not request a compression budget. Standard mode is sufficient for
two author-controlled Proposer/Balancer rounds without delegating work or
turning the design request into an implementation workflow.

## Rejected Alternatives

| Alternative | Why rejected |
| --- | --- |
| live-presentation | The destination is an interactive page, not a spoken deck. |
| marketing conversion page | Participation is a voluntary community ritual, not a sales funnel. |
| number-first explainer | Repeats the confusion risk and can resemble gambling or token reward. |
| ritual-first manifesto | Emotionally resonant but too abstract for first-pass comprehension. |
| implementation plan | The operator requested design and copy; source mutation is not authorized. |

