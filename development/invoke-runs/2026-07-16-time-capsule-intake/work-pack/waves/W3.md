# W3 — Rehearse And Decide

Layer: L3

Status: blocked by W2 promotion evidence

## Layer Question

After this wave, we know whether a bounded ceremony can be deployed, reproduced,
restored, and responsibly approved without hidden token or authority effects.

## Tasks And Order

1. TASK-TC-007: SWU-TC-013 -> SWU-TC-014.
2. TASK-TC-READINESS-008: closure-only audit after all receipts exist.

## Exit Evidence

- pinned staging build completes the entire synthetic ceremony;
- network, logs, storage, and permissions match the approved data boundaries;
- independent verifier reproduces root and supply without private state;
- successor cold restore preserves root, consent, and history;
- no component contains mint authority;
- every required owner returns an evidence-linked accept/bound/reject decision.

## Promotion

The audit returns `launch`, `narrow`, or `stop` with an expiry/rollback condition.
Only the external authority owner may act on it. This work-pack never launches
or mints by itself.

