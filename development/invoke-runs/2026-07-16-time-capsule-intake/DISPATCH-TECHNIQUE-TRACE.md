# Dispatch Spec Technique Trace

Full dispatch JSON: not required. This route is sequential lifecycle authoring
with one later Task Session owner, no delegated execution, and no real protected
content. A future real-intake route must re-evaluate full-dispatch need because
it crosses security, privacy, admission, token, and stewardship owners.

| Technique | Trigger | Applied to | Validation expectation | Status |
| --- | --- | --- | --- | --- |
| `sequence` | User requested define, design, then plan. | Define outputs feed design; approved design refs feed plan. | Every later artifact links prior handles. | pass |
| `frame_handoff` | Public explanation must remain safe across technical and community views. | `SPEC.md` -> `FORM-COPY.md` and `UX-PLAN.md` | Copy preserves boundaries without importing raw research prose. | pass |
| `handle_handoff` | Plan must not duplicate owner artifacts. | Work-pack source anchors | Tasks refer to concrete artifacts by path. | pass |
| `residue_ledger` | Several live-intake decisions remain owner-gated. | `DECISIONS-AND-GAPS.md`, work-pack blockers | Every gap has an owner and repair path. | pass |
| `owner_boundary_check` | Token, identity, privacy, and archive authority are external. | all three stages | Invoke claims authoring only; Task Session owns execution. | pass |
| `artifact_contract_bridge` | Form, algorithm, tasks, and waves need checkable contracts. | Design and plan outputs | Required fields, algorithms, and validation surfaces are explicit. | pass |
| `validation_loop` | No mechanism fixture has run. | layering and work-pack | Each layer promotes only from named evidence. | pass |
| `concrete_path_evidence` | Dirty repository and superseded prior plan require precise sources. | context and SWU anchors | Existing evidence uses concrete file paths and selectors. | pass |
| `scu_swu_reduction` | High-complexity plan needs an executable first unit. | Distill + work-pack | First unit is `SWU-TC-001`, not the whole capsule. | pass |
| `recomposition_proof` | A small form unit must still serve the complete product. | Distill validation | Six-slot UX recomposes into envelope, manifest, root, supply, and releases. | pass |
| `execution_receipt_handoff` | Later Task Sessions must return evidence. | task contracts | Each SWU names expected result fields and validation. | pass |
| `authority_split_gate` | Later layers cross multiple owner boundaries. | L2/L3 gates | Unresolved owner approval blocks those layers, not L0 design proof. | pass |

Skipped:

- `fanout`, `dialectic`, and `tournament`: no subagent strategy was requested or needed;
- `observability_grouping`: one local authoring route, no sibling dispatch steps;
- `toy_game`: named as downstream evidence work, not executed during Invoke authoring.

