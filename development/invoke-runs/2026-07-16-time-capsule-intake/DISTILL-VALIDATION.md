# Automatic Distill Validation — Invoke Plan

Mode: `validate`

Role path: parent-local Proposer/Balancer trace; no subagents were requested

Verdict: `pass` for the navigable L0 plan; overall Invoke result remains `flag`
because real intake has later owner gates.

## Objective And Output

- Objective: make the three-horizon, two-word contribution model understandable
  and executable in progressive evidence layers.
- Output: split work-pack, layer model, tasks, SWUs, waves, and validation.
- Budget: Validate; one Balancer-led pass and one repair reconciliation.

## Role Conversation

- Proposer: use the complete synthetic capsule as the first unit.
- Balancer objection — scope: root, beacon, quota, sealed content, stewardship,
  and form together are too large and would hide missing owners.
- Reconciliation: L0 becomes public explanation + non-collecting six-slot form +
  schema/fixture contract; mechanics, real intake, sealed content, and ceremony
  promote only through later layers.
- Stable disagreement: none for L0. Real band, beacon, admission, crypto,
  moderation, and stewardship remain owner gaps rather than design ambiguity.

## Smallest Coherent Unit

`SWU-TC-001`: change the public explanation so a new participant can retell:

```text
up to two words per 2/5/10-year opening
  -> all accepted places form one locked capsule
  -> later public value
  -> one starting supply
  -> later openings do not change the number
```

It is smaller than the form implementation but remains meaningful because it
owns the current comprehension failure and provides the copy contract the form consumes.

## Recomposition Proof

`SWU-TC-001 -> SWU-TC-002` produces the user surface. The form produces a
six-slot envelope; schemas turn it into accepted records; mechanics produce the
manifest/root/supply; lifecycle tasks return the records at 2/5/10 years. No
hidden glue or alternate product is required.

## Checks

| Check | Result | Evidence or gap |
| --- | --- | --- |
| SCU/SWU boundary | pass | SWU-TC-001 has one copy outcome, concrete paths, and review checks. |
| Recomposition | pass | copy -> form -> envelope -> records -> root -> supply -> releases. |
| Hidden acceptance gaps | pass with gates | B-01–B-07 have owners and block only affected layers. |
| Overbuilt abstraction | pass | no generic form platform, identity system, token engine, or custom crypto in L0. |
| Task detail | pass | execution tasks name inputs, outputs, algorithms/states, edge cases, and checks. |
| SWU manifest | pass | 14 unique, singly parented SWUs; readiness task has valid exemption. |
| Navigation | pass | start at WORK-PACK -> TASK-TC-001 -> SWU-TC-001. |

## Technique Trace

- abstraction-level guard: product, form, mechanism, and lifecycle remain distinct;
- recomposition proof: pass;
- evolution profile: successor rules version for changed horizons/quota;
- boundary-object check: “word slot” stays stable across UX and records;
- premortem: guard against live collection after only the form looks ready;
- frame expiry: rerun if quota, horizons, consent, supply effect, or admission model changes;
- navigable result: pass.

## Next Route

Task Session for `SWU-TC-001` after explicit user approval. No other SWU is selected.

