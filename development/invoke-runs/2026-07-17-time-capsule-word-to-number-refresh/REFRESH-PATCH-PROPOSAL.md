# Refresh Patch Proposal

Mutation mode: `proposal-only`

No target artifact is edited by this file. It identifies the smallest changes
for operator review and a later bounded Task Session.

## Target: `ARCHITECTURE.md`

### Replace

- Design Intent's primary model “The capsule chooses one beginning and returns
  three times.”
- View 2 sections `Ten-Second Hero`, `Memorable Visual Equation`, and
  `Compact Mechanism Disclosure`.
- View 3 `MechanismDisclosure` as the primary mechanism surface.

### With

- Primary model: “Every word becomes a record. Every record changes the
  capsule fingerprint. The fingerprint and a later public value select the
  number inside the agreed range.”
- First-field visible pipeline and `TransformationExplorer` from
  `MECHANISM-CLARITY-DESIGN.md`.
- “One beginning, three returns” retained as the bridge after causality.

### Signal mapping

RS-01, RS-02, RS-03.

## Target: `UX-PLAN.md`

### Replace

- First visual field ordering that places the mechanism in supporting detail.
- Closed-by-default mechanism explanation pattern.

### With

1. mechanism question and causal answer;
2. visible five-node pipeline;
3. computed local-only transformation explorer;
4. 2/5/10-year bridge;
5. participation form.

Add the seven-question mechanism comprehension gate from
`MECHANISM-CLARITY-DESIGN.md`.

### Signal mapping

RS-01, RS-03.

## Target: `WEB-COPY.md`

### Replace

- `Hero`;
- `Hero-to-Form Bridge`;
- `Mechanism Disclosure`;
- the mechanism paragraph in `Rehearsal Receipt`.

### With

The matching sections in `REVISED-MECHANISM-COPY.md`. Keep form validation,
visibility, review, safety, footer, and no-script states unchanged unless a
specific replacement is named.

### Signal mapping

RS-01, RS-02, RS-03.

## Target: `COMPOSITION-PLAN.md`

### Replace

Candidate A `Memory First` as selected.

### With

Candidate C `Visible Transformation First` from
`MECHANISM-COMPOSITION-PLAN.md`. Preserve Memory First as the emotional bridge,
not the opening structure.

### Signal mapping

RS-01, RS-03.

## Target: `WHISPER-VALIDATION.md`

### Change

The prior `whole-capsule effect: pass` becomes `reopened / pending human
evidence`. Author inspection proved that the phrase existed, not that people
understood the transformation.

### Add

- pipeline completeness check;
- no-direct-word-value check;
- later-value-purpose check;
- computed-example truth check;
- seven-question human mechanism retell gate.

### Signal mapping

RS-01, RS-02.

## Target: Current HTML

No change in this refresh. A later Task Session may implement only after
operator approval. The implementation must:

- compute the toy record/root/result from declared invented fixtures;
- make zero network or persistence calls;
- retain the existing rehearsal boundary;
- pass browser, keyboard, responsive, and comprehension-fixture validation;
- avoid production range, beacon, token, or cryptographic claims.

