# Dispatch Spec Technique Trace

Full dispatch: `DISPATCH.json`

Full JSON is required because the correction crosses Context Builder,
Inventory, Invoke Refresh, and Whisper, then creates a reusable implementation
proposal. No subagent strategy is used.

| Technique | Trigger | Applied to | Validation expectation | Status |
| --- | --- | --- | --- | --- |
| `sequence` | Correction evidence must flow into refresh and composition. | s01-s05 | Every non-first phase consumes an earlier frame, handle, or artifact. | pass |
| `frame_handoff` | Whisper must receive mechanism and claim boundaries, not only the user's short correction. | s01, s04 | Causal copy matches approved mechanism evidence. | pass |
| `handle_handoff` | Inventory and target artifacts must remain traceable. | s01-s02 | Concrete prior-package and inventory handles are retained. | pass |
| `concrete_path_evidence` | Refresh cannot guess which design/copy is stale. | s01-s02 | All target artifacts and mechanism sources have paths. | pass |
| `x_ray` | The failed explanation hides several type changes. | s03 | `MECHANISM-CLARITY-DESIGN.md` exposes word, record, root, later value, and range result. | pass |
| `artifact_contract_bridge` | Refresh, copy, and future demo need checkable outputs. | s03-s05 | Proposal, copy surfaces, fixture truth gate, and validation are explicit. | pass |
| `route_projection_boundary` | Interactive web copy is a candidate Whisper extension. | s04 | Transport status remains candidate/unproven. | pass |
| `residue_ledger` | Provider, range, fixture proof, and human comprehension remain open. | s03, s05 | Each gap stays visible with a next owner. | pass |
| `validation_loop` | A prior self-reviewed pass did not match user evidence. | s05 | Whole-capsule comprehension is reopened and requires retell evidence. | pass |
| `authority_split_gate` | Refresh cannot implement, select production inputs, or validate people. | s03, s05 | Operator, Task Session, mechanism owners, and participant test keep authority. | pass |

Skipped:

- `fanout` and delegated `dialectic`: no subagent work was requested;
- `tournament` as dispatch topology: Whisper records a local candidate set but
  the route remains parent-sequential;
- `toy_game`: a toy computation is specified for the next Task Session, not
  executed during proposal-only refresh;
- `execution_receipt_handoff`: no delegated execution occurs.

