# Closure Gates

The selected protocol-primary hybrid may be described as operatorless during its
normal opening path only when all applicable blocking gates pass.

| Gate | Acceptance condition | Blocking |
|---|---|---|
| G-01 Timelock | Near-future fixture cannot be decrypted early and can be decrypted after its verified target round. | yes |
| G-02 Chain pinning | Chain hash, public key, genesis, period, and target-round calculation are fixed in the manifest and independently verified. | yes |
| G-03 Static recovery | A clean browser recovers a fixture with the application API and original domain unavailable. | yes |
| G-04 Private return | Correct participant recovery kit opens the due inner ciphertext; wrong key does not; permanent-loss copy is accepted. | yes if private return launches |
| G-05 Determinism | Browser and server generate identical commitments from the same canonical fixture. | yes |
| G-06 Storage | Encrypted fixture is retrieved and byte-verified through independent Arweave paths. | yes |
| G-07 Irreversibility | Public-at-opening copy and moderation closure explicitly acknowledge that due plaintext cannot be recalled. | yes |
| G-08 Ten-year risk | Decision owner explicitly accepts or mitigates the documented 5–10-year and non-quantum-resistant boundary. | yes |
| G-09 Recovery package | Source, built decoder, dependency lock, test vectors, manifest, hashes, transaction IDs, and plain-language instructions are archived together. | yes |
| G-10 Close rehearsal | A full disposable rehearsal produces and recovers all 2/5/10-year package shapes. | yes |

## Fail-closed rule

If any blocking gate is not satisfied before the real close:

- do not describe the release as operatorless;
- do not discard the selected emergency steward recovery path;
- do not place the only recoverable copy behind the unvalidated protocol;
- preserve the current entrusted-capsule boundary and record the autonomous path as
  a future migration experiment.
